services:
  redis:
    image: redis:7-alpine
    container_name: redis
    command: ["redis-server", "--requirepass", "${REDIS_PASS}", "--save", "20", "1", "--loglevel", "warning"]
    volumes:
      - ../../data/redis:/data
    networks: [homelab]
    restart: unless-stopped
